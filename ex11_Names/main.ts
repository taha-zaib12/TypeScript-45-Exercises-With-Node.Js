//11.Names: Store the names of a few of your friends in a array called names. Print each person’s name by accessing each element in the list, one at a time.

let friendsName = ["Usama","Sami","Kumail","Usman"];
 friendsName.forEach(friendName => console.log(friendName));   // .foreach() function is used to take values from an array one by one and 
                                                                    //print them each one by one.

